# csust-news
# 10.12

- 完成对该项目的顶部自定义导航栏以及底部tabbar。

- 实现顶部自定义导航栏与内容双向切换绑定

  总结如下：

  1. 对于页面自定义导航以及tabbar不熟悉。导致于内容始终与顶部导航产生冲突，最后通过设置高度以及利用overflow完美解决。
  2. 实现上下滑动时，需要对于scroll以及scroll以上view标签设置高度。以实现滑动效果
  3. 对于swiper组件，需要利用change事件以及current属性 与顶部navbar进行索引事件交互以达到双向切换绑定效果。

# 10.13

- 完成对项目底部+号点击按钮来弹出发布文章与视频按钮

  总结如下

  1. 布局问题导致各个设备稍微不兼容(需要后期完善)。
  2. 组件封装对于冒泡框下方三角指示主要通过两层倒三角叠加并且通过定位来确定位置。

# 10.15
- 完成对项目设备属性的保存以及对于导航tabs粘性布局的修改、文章卡片的编写

  总结如下

  1. position:sticky; 需指定距离top位置属性。
  2. 利用Vuex模块化，来保存设备状态至一个模块。以达到模块化管理

# 10.15
- 完成对项目热播模块的编写以及完成，解决切换tab返回顶部、

  总结如下

  1. OnpageScroll生命周期用来监听页面的滚动事件，但是他不能监听scroll-view。监听scroll-view需要用到scroll-view自身的scroll + scrollTop来实现。
  2. scroll-view中scroll-top无效果问题，可以借助官网来解释：[地址如下](https://uniapp.dcloud.net.cn/tutorial/vue-api.html#componentsolutions)
  3. 对于OnpageScroll可以通过uni.OnpageScroll函数更好的去完成返回顶部功能

# 10.18

- uniapp分包

- 完成对于搜索界面中搜索框以及对于搜索框触发事件进行一系列逻辑处理和组件切换。

  总结如下：

  1. uniapp中pages.json 中的subPackages可用来分包。分包可以用来加快小程序运行速度以及优化用户体验(根据用户操作来下载包资源)。
  2. 在封装组件的过程中，尽量把组件内回调函数暴露给父组件进行操作。
  3. 在编写代码过程中，尽量不使用无意义字符串(可以将字符串封装成常量来操作)。
  4. 在利用组件父子间传递参数时，可以将传入相关配置对象来实现组件的个性化定义。
  5. 多看导入组件源码，利用代码调试查找微小bug。

  ​	
  
  # 10.21
  
  - 完成搜索界面的历史以及热搜的基本逻辑，并且利用vuex模块化做持久化存储。
  - 美化了+号上传视频的弹出框。
  - 今日主要研究富文本编辑器的封装以及使用
  
  # 11.10
  
  - 富文本图片上传下方出现大部分留白以至于软键盘弹出页面整体往上移动(待解决)
  - 对接部分接口，是页面逻辑完整。
  - 编写详情页